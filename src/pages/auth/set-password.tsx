import { AlliedWorldLogo, AuthLayout, SetPasswordForm } from '@/components/auth';

const SetPasswordPage = () => {
  return (
    <AuthLayout>
      <AlliedWorldLogo />
      <SetPasswordForm />
    </AuthLayout>
  );
};

export default SetPasswordPage;
