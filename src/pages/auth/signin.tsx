import { AlliedWorldLogo, AuthLayout, SignInForm } from '@/components/auth';

const SigninPage = () => {
  return (
    <AuthLayout>
      <AlliedWorldLogo />
      <SignInForm />
    </AuthLayout>
  );
};

export default SigninPage;
