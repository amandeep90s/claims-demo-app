import { AlliedWorldLogo, AuthLayout, SignUpForm } from '@/components/auth';

const SignupPage = () => {
  return (
    <AuthLayout>
      <AlliedWorldLogo />
      <SignUpForm />
    </AuthLayout>
  );
};

export default SignupPage;
