import { AlliedWorldLogo, AuthLayout, ForgotPasswordForm } from '@/components/auth';

const ForgotPasswordPage = () => {
  return (
    <AuthLayout>
      <AlliedWorldLogo />
      <ForgotPasswordForm />
    </AuthLayout>
  );
};

export default ForgotPasswordPage;
